import { Text, View } from "react-native";
import { ScreenContainer } from "@/core";

export const PostScreen = () => {
  return (
    <ScreenContainer>
      <Text>Post</Text>
    </ScreenContainer>
  );
};
