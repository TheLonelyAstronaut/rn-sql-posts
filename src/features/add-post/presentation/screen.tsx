import { ScreenContainer } from "@/core";
import { Text, View } from "react-native";

export const AddPostScreen = () => {
  return (
    <ScreenContainer>
      <Text>Add Post</Text>
    </ScreenContainer>
  );
};
